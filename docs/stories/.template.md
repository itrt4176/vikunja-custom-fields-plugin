<!--
  Story template — fill out each section before the story enters its own
  brainstorming → planning → implementation cycle.

  GUIDELINES:
  - Describes WHAT and WHY. Never HOW.
  - No implementation details: no routes, no schemas, no component names,
    no file paths, no code, no pseudocode, no library choices.
  - Each story delivers one concrete, user-verifiable outcome.
  - Anything that applies to every story (e.g. "zero core changes") stays
    in the PRD — don't repeat it here.
-->

---
title: ""
description: ""
status: pending
priority: 0
labels: []
position: 0
---

<!--
  YAML frontmatter fields — all map to native Vikunja task properties for
  trivial import once the server is up:

  title       → Task.Title              (same as the # heading below)
  description → Task.Description        (one-line summary; appears in Vikunja card/list views)
  status      → derived                 (pending→todo, in-progress→in-progress, complete→completed)
  priority    → Task.Priority           (int64, 0 = unset, higher = more important)
  labels      → Task.Labels             (string array, e.g. ["backend", "frontend", "infrastructure"])
  position    → Task.Position           (float64, controls ordering within the project)
-->

# [Story Title]

<!--
  One-line name. Imperative, user-centric. Must match the title in frontmatter.
  Good: "Admins can manage field definitions via the API"
  Bad:  "Field definition CRUD routes"
-->

## Outcome

<!--
  What can a user do after this story is done that they couldn't do before?
  One to three sentences. Concrete and verifiable.

  If you can't describe the outcome in terms of what a USER can DO, the
  story is probably too low-level (implementation task) or too vague.
-->

## What & Why

<!--
  What are we adding? Why does it matter? One to three paragraphs.

  Stay at the PRD's altitude. "Exposes an API for field definition CRUD"
  is fine. "POST /custom-fields with an xorm session" is not.

  If you need more than three paragraphs, the story is probably doing too
  much — consider splitting it.
-->

## Design Principles

<!--
  Which PRD design principles does this story advance? Copy the relevant
  ones verbatim from the PRD and add a one-sentence explanation of how
  this story serves each.

  Example:
  - "API-first, UI-second" — this story establishes the API surface that
    later stories consume.
-->

## Dependencies

<!--
  - **Must come after:** [list story titles or "none"]
  - **Must come before:** [list story titles or "none"]
  - **Can run in parallel with:** [list story titles or "none"]

  "Can run in parallel" means no shared artifacts — two stories that
  modify the same file or depend on the same intermediate design decision
  should NOT be marked parallel, even if they could theoretically be
  worked on simultaneously.
-->

## Acceptance Criteria

<!--
  Concrete, verifiable conditions that mean the story is done.
  Each criterion should be testable — manually or automated.

  Good: "Creating a text field assigned to Project A makes it appear in
         the API response for Project A's tasks and NOT for Project B's."
  Bad:  "The API works correctly."

  Aim for 3–7 criteria. If you need more than 7, the story is probably
  doing too much.
-->

1.
2.
3.

## Scope

<!--
  IN for this story — the boundaries of what will be delivered.
  OUT for this story — adjacent concerns this story explicitly defers.

  Example:
  - IN:  CRUD operations on field definitions.
  - OUT: Assigning values to tasks — that's a separate story.
  - OUT: The admin UI for managing fields — that's a separate story.
-->

**In scope:**
-

**Out of scope:**
-